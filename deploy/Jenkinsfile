pipeline {
    agent any

  stages {
    stage('SCM Checkout') {
      steps {
        git branch: 'main', url: 'https://github.com/somex6/ansible-config-mgt.git'
      }
    }
  }

  stages {
    stage('SCM Checkout') {
      steps {
        withEnv(['ANSIBLE_CONFIG = ${WORKSPACE}/deploy/.ansible.cfg']) {
          ansiblePlaybook credentialsId: 'private-key', disableHostKeyChecking: true, installation: 'ansible2', inventory: 'inventory/dev.yml', playbook: 'playbook/site.yml'
        }
      }
    }
  }
}
