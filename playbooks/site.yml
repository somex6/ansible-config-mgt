- name: Include dynamic variables
  hosts: all
  tasks:
  - include_vars: ../dynamic-assignment/env_vars.yml
  tags:
    - always

- name: Loadbalancers assignment
  hosts: lb
- import_playbook: ../static-assignments/loadbalancers.yml
  when: load_balancer_is_required 

